#include <stdlib.h>
#include <locale.h>
#include <stdio.h>

int main(int argc, char *argv[])
{
  setlocale(LC_ALL, "Portuguese");
  /*   printf("Bem vindo ao mundo da Linguagem C!\n");

    int num_1 = 30 / 2,
        num_2 = 30,
        soma = num_1 + num_2;

    if ((num_1 + num_2) == 60)
    {
      printf("A soma de %d + %d é igual a %d, esse valor é igual a 60", num_1, num_2, soma);
    }
    else
    {
      printf("A soma de %d + %d é igual a %d, esse valor é diferente de 60", num_1, num_2, soma);
    }

    return 0;
  } */